import 'package:flutter/material.dart';

//proxys

class WBTabIndicator extends Decoration {
  WBTabIndicator(
      {Color color, int count, double sliderWidth, EdgeInsets edgeInsets});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }

  @override
  BoxPainter createBoxPainter([void Function() onChanged]) {
    // TODO: implement createBoxPainter
    throw UnimplementedError();
  }
}

class WBListView extends StatelessWidget {
  WBListView({
    String key,
    String data,
    Widget children,
  });

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}

class WBImage extends StatelessWidget {
  WBImage({
    String url,
    Size size,
    String iconName,
  });
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}

class Stateful extends StatelessWidget {
  Stateful({Widget child});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
